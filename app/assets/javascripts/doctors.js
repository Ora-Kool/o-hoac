$(document).ready(function () {
    $('#more').hide();
    $('.inactive, .default, .in-active').click(function (event) {
        event.preventDefault();
    })

});

function check() {
    $('a').slideDown;
}
