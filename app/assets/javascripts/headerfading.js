window.addEventListener("scroll", function() {
    if (window.scrollY > 500) {
       // $('.top-bar ul').addClass('opaque');
        //$('.top-bar').addClass('opaque');


    }
    else {
       // $('.top-bar ul').removeClass('opaque');
        //$('.top-bar').removeClass('opaque');
    }
},false);