# @ApptPoller =
  poll: ->
    #setInterval @request, 5000
  request: ->
#$.get($('#new_appointments').data('url'))
    #$.get($('#new_appointment').data('url'))

##jQuery ->
 ## if $('#new_appointment').length > 0
 ##   ApptPoller.poll()



